<!-- <div class="info">
<%= form_for @user_signup, {url: users_path}  do |u| %>
		<%= u.text_field :name, placeholder: "create a username" %><br />
		<%= u.password_field :password, placeholder: "create a password" %><br />
		<%= u.password_field :password_confirmation, placeholder: "retype the password" %>
		<%= u.submit "Creat My Account!" %>
		<br />
	<%= link_to "Back to the homepage!", foodiepictures_path %>
</div>
<% end if !current_user %> -->

<div>
<%= form_for @user_signup, {url: users_path} do |u| %>
	<form class="form-horizontal" role="form">
	  <div class="form-group btn-pink btn-block">
	    <div class="col-sm-12">
	     <%= u.text_field :name, placeholder: "create a username", class: "form-control" %>
	    </div>
	  </div>
	  <br /><br />
	  <div class="form-group btn-pink btn-block">
	    <div class="col-sm-12">
	      <%= u.password_field :password, placeholder: "create a password", class: "form-control" %>
	    </div>
	  </div>
	  <br /><br />
	  <div class="form-group btn-pink btn-block">
	    <div class="col-sm-12">
	      <%= u.password_field :password_confirmation, placeholder: "retype password", class: "form-control" %>
	    </div>
	  </div>
	  <br /><br />
	  <div class="form-group">
	    <div class="col-sm-12">
	      <%= u.submit "Creat My Account!", class: "btn btn-pink btn-block" %>
	    </div>
	  </div>
	</form>
	<% end if !current_user %>
</div>